const getMovieCount = (items) => {
  const result = items.length;
  return result;
};

const getCommentCount = (comments) => {
  const res = comments.length;
  return res;
};

export { getMovieCount, getCommentCount };
